# Vim Tool

### 設定方法
```
git clone https://github.com/TomoProg/VimTool
cd VimTool
ln -s "`pwd`/.vimrc" ~/.vimrc
ln -s "`pwd`/filetype/filetype.vim" ~/.vim/filetype.vim
ln -s "`pwd`/filetype/ftplugin" ~/.vim/ftplugin
```
